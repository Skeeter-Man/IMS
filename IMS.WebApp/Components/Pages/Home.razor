@page "/"

@inject IViewInventoriesByNameUseCases ViewInventoryByNameCase
@inject IEditInventoryUseCase EditInventoryUseCase

<PageTitle>Test Page</PageTitle>
<br />
<br />

<ul>
    @if (inventories is not null && inventories.Count > 0)
    {
        foreach (var inv in inventories)
        {
            <li>
                @inv.InventoryName @inv.Quantity @inv.Price
            </li>
        }
    }
</ul>


@code{
    private List<Inventory>? inventories;

    protected override async Task OnInitializedAsync()
    {
        //var inventory = new Inventory { InventoryId = 1, InventoryName = "Bike Seat", Quantity = 50, Price = 3 };
        //await EditInventoryUseCase.ExecuteAsync(inventory);

        //inventories = (await ViewInventoryByNameCase.ExecuteAsyn()).ToList();
    }

}